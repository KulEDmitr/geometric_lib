name: Run_tests
on: push
jobs:
  lint:
    runs-on: [ubuntu-latest, windows-latest]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: test
        uses: |
          /home/littleking08/ИСРПО/geometric_lib/build/tests/tests
