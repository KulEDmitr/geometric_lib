name: CI
on:
  push:
    branches: [ "docs_466980" ]
  pull_request:
    branches: [ "docs_466980" ]
  workflow_dispatch:
jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Run Tests
        run: python -m unittest test.py
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Run Tests
        run: python -m unittest test.py
