name: Run_tests
on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
  tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: Run tests
        run: |
          build/tests/tests
