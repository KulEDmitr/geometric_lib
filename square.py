def area(a: int) -> int:
    """
    Gets side of square, returns area

    Example:
        square.area(2) = 4
    """
    return a * a


def perimeter(a: int) -> int:
    """
    Gets side of square, returns perimeter

    Example:
        square.perimeter(2) = 8
    """
    return 4 * a
