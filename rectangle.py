def area(a, b): 
    # Принимает на вход длины сторон прямоуголька a и b, возвращает площадь прямоугольника
    return a * b 
    # a = 4, b = 5, return = 20
def perimeter(a, b): 
    # Принимает на вход длины сторон прямоуголька a и b, возвращает периметр прямоугольника
    return (a + b)*2 
    # a = 4, b = 5, return = 18